const BackgroundCircle=(props)=>(
    <circle
            r={props.radius}
            fill="yellow"
            stroke="black"
            strokeWidth={props.strokeWidth}
          />
  )

  export default BackgroundCircle;